# This is a Databricks asset bundle definition for my_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: my_project
  uuid: 1e618c11-664f-4790-90a8-f3f6b28bab03

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f122dc18-1b68.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-f122dc18-1b68.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/prakashsvks88@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/prakashsvks88@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: prakashsvks88@gmail.com
        level: CAN_MANAGE
